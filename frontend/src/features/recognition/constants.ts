// Minimum confidence score to accept a recognition result
export const RECOGNITION_CONFIDENCE_THRESHOLD = 0.75;

// Interval between recognition attempts (ms)
export const RECOGNITION_POLL_INTERVAL_MS = 1500;

// Max canvas preview width for camera feed
export const CAMERA_PREVIEW_MAX_WIDTH = 1280;
